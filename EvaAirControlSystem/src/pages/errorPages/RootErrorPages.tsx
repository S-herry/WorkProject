const RootErrorPages = () => {
  const title: string = "Error";
  const message: string = "頁面錯誤";

  return (
    <div>
      <h3>{title}</h3>
      <p>{message}</p>
    </div>
  );
};

export default RootErrorPages;
